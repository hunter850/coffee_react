import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Points = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>積分</h2>
        </Fragment>
    )

    return el;
}

export default Points