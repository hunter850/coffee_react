import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Getcoupon = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>獲得優惠卷</h2>
        </Fragment>
    )

    return el;
}

export default Getcoupon