import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Getpoint = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>獲得積分</h2>
        </Fragment>
    )

    return el;
}

export default Getpoint