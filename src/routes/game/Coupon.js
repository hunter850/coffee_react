import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Coupon = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>優惠卷</h2>
        </Fragment>
    )

    return el;
}

export default Coupon