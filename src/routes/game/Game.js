import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Game = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>遊戲</h2>
        </Fragment>
    )

    return el;
}

export default Game