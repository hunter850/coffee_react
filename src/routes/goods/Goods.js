import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Goods = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>商品</h2>
        </Fragment>
    )

    return el;
}

export default Goods