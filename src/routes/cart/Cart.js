import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Cart = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>購物車</h2>
        </Fragment>
    )

    return el;
}

export default Cart