import { Fragment } from "react";
import NavBar from "../../component/NavBar";

function Reserve() {
    return (
        <Fragment>
            <NavBar />
            <h2>訂位</h2>
        </Fragment>
    );
}

export default Reserve;
