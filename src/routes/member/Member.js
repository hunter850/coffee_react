import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Member = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>會員</h2>
        </Fragment>
    )

    return el;
}

export default Member