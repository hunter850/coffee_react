import { Fragment } from "react";
import NavBar from "../../component/NavBar";

const Lesson = () => {
    const el = (
        <Fragment>
            <NavBar />
            <h2>課程</h2>
        </Fragment>
    )

    return el;
}

export default Lesson